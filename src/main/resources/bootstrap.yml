spring:
  application:
    name: ${SERVICE_NAME:profile_service}
  cloud:
    config:
      enabled: false

#Не знаю, нужно или нет
#mdp.discovery:
#  discoveryClientName: ${DISCOVERY_CLIENT_NAME:eureka}